function noty(title,content,theme,life=1500,sticky = false) {
    $.notific8(content, {
        life: life,
        heading: title,
        theme: theme,
        sticky: sticky,
        horizontalEdge: 'top',
        verticalEdge: 'right',
        zindex: 1500
    });
}